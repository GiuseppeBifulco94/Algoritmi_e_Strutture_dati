#ifndef BSTLIST_H
#define BSTLIST_H
#include "fun_men.h"

struct node * mergetree(int key, struct node *root1, struct node * root2){  //Mergetree unisce due alberi
    struct t0 = (tree)malloc(sizeof(dim));
    t0->T_l = t1;
    t0->T_r = t2;
    t0->value = el;
    return(t0);
}

struct node * createleaf(int key){
    return mergetree(key, NULL, NULL);  //Ogni foglia è formata da un valore e due puntatori nulli
}


}

#endif
